# Platform Admin Panel

Retired. `/platform` is not a live operator console on this self-hosted fork.

`proxy.ts` no longer runs a super-admin check for those paths. The school admin dashboard is `/dashboard/admin`. Do not import `components/platform-sidebar.tsx`. That file is gone.
